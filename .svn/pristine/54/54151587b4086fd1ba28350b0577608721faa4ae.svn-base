# Server version: 4.0.13
# PHP Version: 4.3.3
# 
USE eseData;

# Create Table structure for table `userlog`

DROP TABLE IF EXISTS `userlog`;
CREATE TABLE `userlog` (
  `user_id` int(11),
  `userlog_timestamp` timestamp,
  `result` varchar(20),
  `reason` varchar(50), 
  `data` varchar(50),
  PRIMARY KEY  (`user_id`, `userlog_timestamp`)
);
